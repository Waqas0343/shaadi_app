import 'package:flutter/material.dart';

class PaymentVideo extends StatelessWidget {
  const PaymentVideo({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: const Text("Payment Video"),
      ),
    );
  }
}
